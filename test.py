def maxsum(list1):
    index1,index2 = 0,0
    sum = 0
    for i in range(len(list1)-1):
        if list1[i] > 0
            index2+=1
            sum+=i 
        if list1[i] < 0:
            if list1[]

def maxSum(array):
    length = len(array)
    sum=array[0]
    result=array[0]

    for i in range(0,n):
        if sum<0:
            sum=a[i]
        else:
            sum=sum+a[i]
            index11=i
        if sum>result:
            result=sum
            index2=i
            start=start1

    return result,index1,index2

def maxSum(array):
    length=len(array)
    sum=array[0]
    result=array[0]

    for i in range(0,length):
        if sum<0:
            sum=a[i]
        else:
            sum=sum+a[i]
            start1=i

        if sum>result:
            result=sum
            end=i
            start=start1
            
    return result,start,end




    